= link_to(t('txt.main'), genres_path) 
>> 
= @movie.title
#yui-main
  .yui-b
    .yui-g
      = render :partial => @movie
      Imdb:
      = link_to(@movie.imdb_id, imdb_url(@movie.imdb_id))
      %br
      = t('txt.genre') 
      -@movie.genres.each do |genre|
        = link_to(t("genres.#{genre.name}"), genre_movies_path(genre)) unless genre.id == 1
      %br
      = t('txt.year') 
      = @movie.year 
      %br
      = t('txt.votes') 
      = @movie.votes 
      %br
      = t('txt.directors') 
      = @movie.directors 
      %br
      = t('txt.writers')
      = @movie.writers
      %br
      = t('txt.release_date') 
      = @movie.release_date 
      %br
      = t('txt.tagline') 
      = @movie.tagline 
      %br 
      = link_to(t('txt.trailers'), @movie.official_site) if @movie.official_site
.yui-b
  = render :partial => "movies/side_panel" 


